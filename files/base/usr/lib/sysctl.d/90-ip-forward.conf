# Enable IP forwarding so the Tailscale exit node can route tailnet egress
# through this host's interfaces. Required for an exit node (and subnet routes);
# without this the kernel drops forwarded packets and exit routing silently
# fails. Host-wide; acceptable on this single-purpose server.
net.ipv4.ip_forward = 1
net.ipv6.conf.all.forwarding = 1
