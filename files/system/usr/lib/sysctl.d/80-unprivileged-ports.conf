# Allow unprivileged (rootless) processes to bind ports >= 80.
# Required so the rootless Caddy reverse proxy can bind :80 and :443 without
# root or file capabilities. Host-wide; acceptable on a single-admin server.
net.ipv4.ip_unprivileged_port_start = 80
